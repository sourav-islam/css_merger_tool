# CSS Merger Web Tool

This repository contains a Django web application scaffold to integrate the existing `css_merger` engine.

See `config/settings.py` for configuration and `merger/services/merger_service.py` for the integration entrypoint.
